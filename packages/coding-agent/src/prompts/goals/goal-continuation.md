<!-- Hidden continuation steer. role=user, suppressed from visible transcript. -->

继续处理当前目标的工作。

<objective>
{{objective}}
</objective>

预算:
- 已用 token:{{tokensUsed}}
- token 预算:{{tokenBudget}}
- 剩余 token:{{remainingTokens}}
- 已用时间:{{timeUsedSeconds}} 秒

这是自主续行。目标跨轮次持续;绝不围绕更小、更容易或已完成的部分子集重新定义成功。

在调用 `goal({op:"complete"})` 之前,你必须对照当前仓库状态执行完成度审计:

1. **把目标重述为具体的交付物。** 目标成立需要哪些文件、行为、测试、关卡或产物存在?把它们写下来(todo 或你的推理中)。
2. **把每个交付物映射到证据。** 对每个要求,找出能证明它的权威来源:文件内容、命令输出、测试通过状态、PR/issue 状态。
3. **检查实际的当前状态。** 读文件。跑命令。查测试。绝不依赖本会话中先前工作的记忆——仓库可能已经变了。
4. **让验证范围匹配论断范围。** 窄检查(一个文件通过单元测试)不能证明宽论断(功能端到端可用)。
5. **把不确定性当作尚未达成。** 间接证据、部分覆盖、缺失产物,或未经检查就“看起来没问题”,都意味着继续工作。收集更强的证据或做更多工作。
6. **预算耗尽不是完成。** 绝不仅仅因为 token 快用完就调用 complete。如果预算紧张而工作未完成,让目标保持活跃并结束本轮——由用户或运行时决定下一步。

只有当每个交付物都有直接、当前状态的证据证明其已满足时,才调用 `goal({op:"complete"})`。完成调用是承重论断;它结束自主循环并向用户呈现“已完成”报告。

如果工作未完成,继续工作即可。绝不叙述你正在继续——直接执行。
