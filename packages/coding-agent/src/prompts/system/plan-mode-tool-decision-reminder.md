<system-reminder>
计划模式的轮次在没有所需工具调用的情况下结束了。

你现在必须恰好选择一个下一步动作:
1. 调用 `{{askToolName}}` 收集所需的澄清,或
2. 用 `{{writeToolName}}` 把计划 slug/标题(`<slug>`,与 `local://<slug>-plan.md` 匹配)作为纯文本写入 `xd://propose`,以结束规划并请求批准

你绝不在本轮输出纯文本。
</system-reminder>
