<system-reminder>
{{#if forced}}
在实质性工作之前,创建一个分阶段的 todo。

你必须在本轮首先调用 `{{toolRefs.todo}}`。
你必须用单个 `init` 操作初始化 todo 列表。
你必须覆盖从调查到实现和验证的整个请求——而不只是紧挨着的下一步。
任务描述必须是简洁、具体、5-10 个词的标签。
`init` 操作只接受阶段名和任务标签字符串;不要发明任务元数据字段。

`{{toolRefs.todo}}` 成功后,在同一轮继续处理请求。
除非任务状态发生实质性变化,绝不再调用 `{{toolRefs.todo}}`。
{{else}}
可以考虑先调用 `{{toolRefs.todo}}`,用单个 `init` 操作铺开分阶段计划。好的列表覆盖整个请求——从调查到实现和验证——而不只是下一步,任务描述要具体,让未来的一轮无需重新规划即可执行。
有用的列表让每项任务保持简洁、具体、5-10 个词的标签;`init` 操作只接受阶段名和任务标签字符串,所以不要发明额外的任务元数据字段。
如果创建了列表,在同一轮继续处理请求,除非任务状态发生实质性变化,避免重新调用 `{{toolRefs.todo}}`。
{{/if}}
</system-reminder>
