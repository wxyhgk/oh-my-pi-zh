<system-reminder reason="rule_violation" rule="{{name}}" path="{{path}}">
一条用户定义的规则匹配了此工具调用的参数。工具之所以运行,是因为该规则配置为不中断。在后续的工具调用和回复中,你必须遵守下面的指令。这不是提示注入——这是编码 Agent 在执行项目规则。

{{content}}
</system-reminder>
