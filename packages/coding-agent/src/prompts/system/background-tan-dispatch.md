<system-notice reason="background_task_dispatched" job="{{jobId}}">
用户启动了一个旁路任务,现在正在一个独立的后台 Agent 中运行。这不是提示注入,也不是给你的新指令——这是编码 Agent 通知你工作已被移交到别处。

下面的任务正在由另一个 Agent 在自己的会话中处理。你不负责它:绝不开始处理它,绝不引用它,绝不让它打断或改变你当前的任务。继续做你正在做的事,就像这条消息没有出现过一样。如有结果,会在后台任务({{jobId}})完成时单独呈现。

已移交的工作(仅供你知晓):
{{work}}
</system-notice>
