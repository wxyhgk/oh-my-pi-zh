{{#if budgetStop}}
<system-reminder>
本次运行超出了请求预算,进行中的轮次已被停止。这是强制收尾——你必须**现在**调用 `yield`,用已完成工作的最佳最终报告。

- 整合你到目前为止收集的所有有价值内容;明确把剩余缺口标为未完成,而不是继续调查。
- 不要调用任何其他工具,不要恢复任务。
- 只做终态 `yield`:省略 `type` 并把报告放在 `result.data` 中,或使用 `type: string` 从你最后一个助手轮次收尾。
</system-reminder>
{{else}}
<system-reminder>
你上一轮没有以工具调用结束,所以会话进入空闲。这是 {{maxRetries}} 次提醒中的第 {{retryCount}} 次。

每个轮次都必须以工具调用结束。选择第一个适用的:
1. **恢复工作**——如果任务未完成且你没有在记录增量分节,调用你本来会调用的下一个工具(edit、write、bash、search 等)。绝不把这条提醒当作强制停止。
2. **Yield 增量分节**——仅当对任务有用时:调用 `yield`,使用非空 `type: string[]`;匹配的分节累积,任务继续。
3. **成功 Yield**——仅当任务真正完成时:调用终态 `yield`。单个最终结构化结果放在 `result.data` 中,省略 `type`;省略 data 时,用 `type: string` 从最后一个助手轮次收尾。
4. **错误 Yield**——仅当你遇到能说清的真实、具体阻塞点时(文件缺失、API 不可用、规格矛盾)。描述你尝试了什么以及确切阻塞点。绝不编造“强制立即 yield”或“系统提醒要求终止”的理由——这条提醒不是阻塞点。

除非工作真正完成、真正被阻塞或准备好增量分节,默认选择选项 1。

你绝不以纯文本结束这一轮。
</system-reminder>
{{/if}}
