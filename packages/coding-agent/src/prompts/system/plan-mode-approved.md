计划已批准。
{{#if contextPreserved}}
- 上下文已保留。有用时可以使用对话历史;如果与较早探索冲突,计划文件是事实来源。
{{/if}}

<instruction>
执行前你必须读取 `{{planFilePath}}`。
文件内容才是权威计划;可见/压缩的上下文是次要的。
读取失败?报告确切路径和错误,而不是猜测。
读取后,你必须以完整的工具访问权限,一步一步执行计划。
在进入下一步之前,你必须验证每一步。
{{#has tools "todo"}}
读取计划后,用 `todo` 初始化跟踪。
每完成一步,立即更新 `todo`。
如果 `todo` 失败,修复载荷并在继续之前重试。
{{/has}}
</instruction>

<critical>
绝不因为内联计划内容被压缩、过期或不可恢复而停止。读取 `{{planFilePath}}`。
你必须持续推进,直到完成。这很重要。
</critical>
