根据需要的推理量,把下面的编码请求归类到一个档位。

档位:

- trivial — 显而易见、机械式或直接提问(重命名、错别字、单行修改、简单查找)。
- moderate — 真实但局部化的任务(小功能、常规 bug 修复、解释代码)。
- hard — 深入、跨多文件、含糊或棘手的调试或设计。

只回复一个词:trivial、moderate 或 hard。

请求:
{{prompt}}

回答:
