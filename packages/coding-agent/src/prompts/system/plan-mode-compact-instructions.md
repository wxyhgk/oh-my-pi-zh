准备执行已批准的计划。

你必须蒸馏计划模式的讨论。保留:
- 计划理由和明确拒绝的备选方案。
- 关键决定及驱动它们的约束。
- 执行者将需要的已发现文件、符号和代码路径。
- 规划期间表达的用户明确偏好。

你必须丢弃:
- 结果已捕获在计划或上方的工具调用噪音(文件读取、搜索)。
- 已被取代的计划草稿。
- 计划文件中已有的重申上下文。

{{#if planFilePath}}
已批准的计划文件在 `{{planFilePath}}`;它是权威事实来源。
你必须保留这个持久路径,以及执行者必须在压缩后直接读取它这一事实。
{{/if}}
