在你认为任务完成之前,请验证:

- 一致性:如果你在一个地方更改了模式、签名或检查,请 grep 每个需要相同变更的其他调用点或重复副本。只应用到部分匹配位置的修复仍然是失败。
- 范围:如果你的 diff 超出解决问题所需的最小变更,确认你没有改变所报告问题之外的任何情况的行为。优先选择最小且正确的 diff,而不是更广泛的改写。
- 验证:运行问题所在的完整测试模块或文件,而不只是你预期会翻转的那一个测试。破坏兄弟测试的变更不是修复。

在完成这三项检查之前,不要声称任务已完成。
