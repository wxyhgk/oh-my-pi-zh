阻塞直到**一个**被观察的会话完成其当前轮次、超时到期,或你被打断——不是直到全部完成。重新发出以继续等待。

轮次结果通常自行交付;你绝不需要它来接收输出。只在你完全被阻塞、无法指挥任何其他会话时使用。

- `sessions` — 要观察的 id。省略则观察每个有轮次在飞的会话。
- `timeout` — 等待的秒数(默认 30)。

已完成轮次的完整结果(活动轨迹 + 回复)会在这里返回,不会单独重新交付。
