结束一个活跃的检查点。把上下文回卷到它,用你的报告替换中间的探索。

在 `checkpoint` 启动的调查工作之后立即调用。

要求:
- `report` 必须简洁、有事实依据且可操作。
- 包含关键发现、决定和任何未解决的风险。
- 除非必要,避免原始草稿日志。
- 如果检查点活跃,你必须在 yield 之前调用它。

行为:
- 如果没有活跃检查点,此工具报错。如果检查点已经回卷,从保留的报告继续,而不是重试。
- 成功时,会话回卷,把你的报告保留为保留上下文,并关闭检查点。
- 一次成功的回卷对该检查点是终局;重复调用会报错。
