生成或编辑图片。

<instructions>
- 为生成或编辑提供一个详细的 `subject` 提示。
- 使用多个 `input` 时,在 `subject` 中描述每个图片的角色(例如 `Image 1` 用于构图,`Image 2` 用于光照)。
- 对于文字:添加 "sharp, legible, correctly spelled";保持文字简短。
</instructions>
