把一条可复用的经验捕获到长期记忆,并可选地在同一次调用中创建或增强一个托管技能。

在解决完某个其洞见会再次带来回报的事情之后使用:一个不明显的修复、一个你必须去发现的项目约定、一个有效的工作流。

当经验是值得固化为 `SKILL.md` 的可重复*流程*(而不只是事实)时,提供可选的 `skill` 对象。托管技能被写入隔离目录(`~/.omp/agent/managed-skills`),下个会话会像普通技能一样呈现。它们绝不触碰用户编写的技能。frontmatter 由 `name` 和 `description` 生成。

克制且具体地捕获。一条强而可复用的经验胜过几条含糊的。
