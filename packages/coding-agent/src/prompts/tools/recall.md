搜索长期记忆中的相关信息。返回按相关性排序的原始匹配条目。

主动使用——在回答关于过去对话、用户偏好、项目决定,或任何先前上下文会帮助准确性的主题之前。拿不准时,先 recall。

需要特定事实或条目时优先 `recall`。需要在许多记忆上进行综合回答时,改用 `reflect`。

每个结果中的内容都是预览。尾随 `…` 标记截断(`truncated: true`,`full_length` 给出原始大小)。用 `read memory://<id>` 获取整行——任何 `memory_edit update` 之前都必须这样做。
