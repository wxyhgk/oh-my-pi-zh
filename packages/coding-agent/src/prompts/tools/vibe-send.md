向你的一个 worker 会话发送消息(按 `vibe_spawn`/`vibe_list` 的 id)。会话保留其完整对话历史——自然地引用早期工作("现在对另一个模块做同样的事")。

立即返回一个 ack,告诉你消息如何落地:

- `turn` — worker 空闲;一个新轮次开始。其结果完成后自动交付。
- `steered` — worker 处于轮次中途;你的消息作为实时转向注入到运行中的轮次。
- `queued` — worker 处于轮次中途且现在不可转向;你的消息自动作为下一轮运行。

用于跟进、纠正、范围变更和审查请求。绝不再解释先前上下文——会话已经有了。
